/* const arr = [1, 2, 3, 4, 5, 6, 7];
arr.push(8); // last ma add garxa 8 lai
arr.pop(); // last element hataidinxa
arr.unshift(0); //suru ma add gardinxu 0 lai
arr.shift(); //suruko element hataidinxa

console.log(arr.includes(6)); // 6 include xa ki xaina hamro array ma bhanera herxa .. it shows either true or false
console.log(arr.indexOf(7)); //kun index ma cha bhanxa .. yedi xaina bhaney -1 which means xaina

const newArr = arr.join(); // adds all the elements of an array into the string separated by comma

console.log(arr);
console.log(newArr);

//console.log(arr);

 */
/* 
Slice and Splice

//Slice -- can be used to create a copy of an array or return a portion of an array
//      -- does not alter the original array

//example of slice

const cities = ["ktm", "pokhara", "damauli", "bharatpur", "dharan", "dhankuta"];

const newCities = cities.slice(2, 5);

console.log(newCities);
console.log("Original Array", cities);

 

// Splice  -- this method will change the contents of the original array
//         -- used to add or remove elements of an existing array and the return value will be the removed items from the array.

//Basic Syntax
  //  splice(start, optional delete count, optional items to add)


const foods = [
  "momo",
  "chowmin",
  "keema noodles",
  "burger",
  "pizza",
  "sandwich",
];

const newFoods = foods.splice(1, 2);
console.log(newFoods);
console.log("Original Array", foods);
  */
